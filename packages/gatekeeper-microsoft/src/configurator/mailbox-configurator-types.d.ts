/** The Outlook Mailbox configurator has a single fixed choice; the whole capability is the resource. */
export type MailboxConfiguratorValues = {
  mode?: "all" | null;
};

export interface MailboxConfiguratorRpc {}
