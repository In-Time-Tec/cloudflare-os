/** The Microsoft Teams configurator has a single fixed choice; the whole capability is the resource. */
export type TeamsConfiguratorValues = {
  mode?: "all" | null;
};

export interface TeamsConfiguratorRpc {}
