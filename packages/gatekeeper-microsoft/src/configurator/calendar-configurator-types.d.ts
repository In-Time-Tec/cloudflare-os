/** The Outlook Calendar configurator has a single fixed choice; the whole capability is the resource. */
export type CalendarConfiguratorValues = {
  mode?: "all" | null;
};

export interface CalendarConfiguratorRpc {}
