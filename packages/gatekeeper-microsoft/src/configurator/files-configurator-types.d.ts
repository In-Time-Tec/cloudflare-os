/** The OneDrive & SharePoint Files configurator has a single fixed choice; the whole capability is the resource. */
export type FilesConfiguratorValues = {
  mode?: "all" | null;
};

export interface FilesConfiguratorRpc {}
