{
  "name": "@intimetec/fixed-bid-pricing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/pricing.ts"
  },
  "scripts": {
    "build": "tsc && tsc -p tsconfig.template.json && node scripts/build-template.mjs --check",
    "build:template": "node scripts/build-template.mjs --write",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "26.1.0",
    "esbuild": "^0.28.1",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "yjs": "^13.6.31"
  }
}
