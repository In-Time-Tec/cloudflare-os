/**
 * Low-contrast dimensional wordmark used on the empty home screen. The moving highlight is kept
 * deliberately slow so it reads as reflected light on a solid object rather than animated text.
 */
export default function OpenWorkLogo() {
  return (
    <div className="openwork-logo" role="img" aria-label="OpenWork">
      OpenWork
    </div>
  )
}
